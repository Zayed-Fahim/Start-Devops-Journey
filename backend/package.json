{
  "name": "start-devops-journey",
  "version": "1.0.0",
  "main": "src/server.js",
  "license": "MIT",
  "scripts": {
    "build": "prisma generate",
    "dev": "prisma generate && prisma migrate deploy && nodemon --watch src --watch prisma src/server.js",
    "start": "nodemon --watch src src/server.js",
    "start:dev": "prisma generate && prisma migrate deploy && nodemon --watch src src/server.js",
    "start:prod": "prisma migrate deploy && node src/server.js",
    "seed": "node prisma/seed.js",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:status": "prisma migrate status",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.2",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "prisma": "^6.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.6",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.1",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import": "^2.32.0",
    "globals": "^17.11.0",
    "nodemon": "^3.1.7",
    "prettier": "^3.9.6"
  }
}
